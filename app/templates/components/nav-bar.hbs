<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Bookworm</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="nav navbar-nav">
            {{#link-to 'books' tagName='li' class="nav-item"}}
                <a href class="nav-link">Books</a>
            {{/link-to}}
        </ul>
        <ul class="nav navbar-nav">
            {{#link-to 'bookstores' tagName='li' class="nav-item"}}
                <a href class="nav-link">Bookstores</a>
            {{/link-to}}
        </ul>
        <ul class="nav navbar-nav">
            {{!-- {{#if session.isAuthenticated}} --}}
                {{!-- <li class="nav-item"><a class="nav-link" href {{action 'logout'}}>Logout</a></li> --}}
            {{!-- {{else}} --}}
                {{#link-to "login" tagName='li' class="nav-item"}}
                    <a href class="nav-link">Login</a>
                {{/link-to}}
            {{!-- {{/if}} --}}
        </ul>
    </div>
</nav>